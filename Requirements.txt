fastapi
python-multipart
a2wsgi

# The documented local setup is `uvicorn server:app`, which needs this.
# Production does not use it - PythonAnywhere serves WSGI and the app is
# wrapped with a2wsgi above - but without it the setup steps in the README
# fail on a clean machine.
uvicorn

# Payments. Pinned so an install months from now matches the one these were
# tested against. Test tools are in requirements-dev.txt.
stripe==15.4.0
SQLAlchemy==2.0.51
alembic==1.19.1
pydantic-settings==2.15.0
